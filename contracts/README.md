# Contracts

This directory is the future cross-platform boundary for Agent command candidates.

Stage A establishes the `schemas/` and `examples/` directories only. The create-plan DTO, JSON Schemas, and example payloads belong to stage E, after the Domain and Application command shapes are implemented and stable.
